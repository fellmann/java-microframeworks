{
  "name": "spark-poc",
  "version": "1.0.0",
  "description": "Spark POC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless-apigw-binary": "^0.4.4"
  }
}
